debug: true
server:
  port:
spring:
  sql:
    init:
      platform: postgresql
      mode: always
  output:
    ansi:
      enabled: always
  datasource:
    driver-class-name:
    url: jdbc:postgresql://mouse.db.elephantsql.com/dcoseslo
    username: dcoseslo
    password: hu8TlwfDGOF7EE0WRIpFLAVYmQ7hinAP
  jpa:
    show-sql: true
    generate-ddl: true
    defer-datasource-initialization: true
    hibernate:
      ddl-auto: none
logging:
  level:
    org:
      springframework:
        boot:
          autoconfigure: ERROR